#include <stdlib.h>
#include <string.h>
#include <time.h>

void m (void) {
    printf(" - %ld", time(NULL));
}

int main (int argc, char **argv) {
    char *one;
    char *two;
    char *three;
    char *four;
    one = malloc(20);
    two = malloc(20);
    three = malloc(20);
    four = malloc(20);
    one = strcpy(one, argv[1]);
    two = strcpy(two, argv[2]);
    fopen("/home/user/level8/.pass", "r");
    puts("~~");
    return 0;
}